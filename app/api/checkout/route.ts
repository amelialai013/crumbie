import{NextResponse}from"next/server";import Stripe from"stripe";import{z}from"zod";import{products,pickupDates,isDateClosed}from"@/lib/catalog";import{setRecord}from"@/lib/store";
const lineSchema=z.object({productId:z.string(),variantId:z.string(),quantity:z.number().int().min(1).max(99),pickupDateId:z.string()});const schema=z.object({lines:z.array(lineSchema).min(1).max(30)});
export async function POST(req:Request){const parsed=schema.safeParse(await req.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Your cart is invalid."},{status:400});const clean=[];for(const line of parsed.data.lines){const product=products.find(p=>p.id===line.productId);const variant=product?.variants.find(v=>v.id===line.variantId);const pickup=pickupDates.find(d=>d.id===line.pickupDateId);if(!product||!variant||!pickup||product.soldOut||product.soldOutDates?.includes(pickup.id)||isDateClosed(pickup))return NextResponse.json({error:"An item or pickup date is no longer available."},{status:409});clean.push({productId:product.id,productName:product.name,variantId:variant.id,variantLabel:variant.label,unitPrice:variant.price,quantity:line.quantity,pickupDateId:pickup.id,pickupDate:pickup.date,pickupWindow:pickup.window})}if(!process.env.STRIPE_SECRET_KEY)return NextResponse.json({error:"Online checkout is temporarily unavailable. Please try again later."},{status:503});const pendingId=crypto.randomUUID();try{await setRecord("pending-checkout",pendingId,{id:pendingId,lines:clean,createdAt:new Date().toISOString()});const stripe=new Stripe(process.env.STRIPE_SECRET_KEY);const origin=process.env.NEXT_PUBLIC_SITE_URL||new URL(req.url).origin;const session=await stripe.checkout.sessions.create({mode:"payment",currency:"aud",line_items:clean.map(l=>({quantity:l.quantity,price_data:{currency:"aud",unit_amount:Math.round(l.unitPrice*100),product_data:{name:`${l.productName} — ${l.variantLabel}`,description:`Pickup ${l.pickupDate}, ${l.pickupWindow}`}}})),customer_creation:"always",phone_number_collection:{enabled:true},success_url:`${origin}/order-success?session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${origin}/cart`,metadata:{pendingId},payment_intent_data:{metadata:{pendingId}}});if(!session.url)throw new Error("Stripe checkout URL missing");return NextResponse.json({url:session.url})}catch{return NextResponse.json({error:"Online checkout is temporarily unavailable. Please try again later."},{status:503})}}
