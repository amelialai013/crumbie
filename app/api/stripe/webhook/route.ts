import{NextResponse}from"next/server";import Stripe from"stripe";import{getRecord,saveRecord,setRecord}from"@/lib/store";
type Pending={id:string;lines:Array<Record<string,unknown>>;createdAt:string};
export async function POST(req:Request){if(!process.env.STRIPE_SECRET_KEY||!process.env.STRIPE_WEBHOOK_SECRET)return new NextResponse("Not configured",{status:503});const stripe=new Stripe(process.env.STRIPE_SECRET_KEY);let event:Stripe.Event;try{event=stripe.webhooks.constructEvent(await req.text(),req.headers.get("stripe-signature")||"",process.env.STRIPE_WEBHOOK_SECRET)}catch{return new NextResponse("Invalid signature",{status:400})}if(event.type==="checkout.session.completed"){const session=event.data.object;const pendingId=session.metadata?.pendingId;if(pendingId){const existing=await getRecord("stripe-event",event.id);if(!existing){const pending=await getRecord<Pending>("pending-checkout",pendingId);if(pending){const order={id:crypto.randomUUID(),stripeSessionId:session.id,customer:{name:session.customer_details?.name||"",email:session.customer_details?.email||"",phone:session.customer_details?.phone||""},lines:pending.lines.map(line=>({...line,status:"paid",readyNotifiedAt:null})),paymentStatus:"paid",createdAt:new Date().toISOString()};await saveRecord("order",order);await setRecord("stripe-event",event.id,{id:event.id,processedAt:new Date().toISOString()})}}}}return NextResponse.json({received:true})}
